package main;

import view.Login;

public class Main {
    public static void main(String[] args) {
        // Launch the Login UI as the entry point of the application
        Login login = new Login();
        login.setLocationRelativeTo(null);
    }
}
